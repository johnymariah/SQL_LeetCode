
SELECT 
    P.PRODUCT_NAME,
    SUM(O.UNIT) AS UNIT
FROM
    ORDERS O
JOIN
    PRODUCTS P
ON
    P.PRODUCT_ID = O.PRODUCT_ID
WHERE 
   EXTRACT(MONTH FROM O.ORDER_DATE) = '2'
AND 
   EXTRACT(YEAR FROM O.ORDER_DATE) = '2020'
GROUP BY
    P.PRODUCT_NAME
HAVING
   SUM(O.UNIT) >= 100;
