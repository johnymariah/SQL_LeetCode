/* Write your PL/SQL query statement below */



-- GROUP BY USER_ID
-- SUM(DISTANCE)
-- JOIN USERS AND RIDES


SELECT U.NAME, NVL(SUM(R.DISTANCE),0) AS TRAVELLED_DISTANCE
FROM RIDES R RIGHT JOIN USERS U
ON R.USER_ID = U.ID
GROUP BY U.NAME
ORDER BY NVL(SUM(R.DISTANCE),0) DESC, U.NAME ASC;
